export interface TodoItem {
  userId: string
  contactId: string
  createdAt: string
  name: string
  gender: string
  mobile: string
  email: string
  dueDate: string
  done: boolean
  attachmentUrl?: string
}
